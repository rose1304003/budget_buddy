# Telegram Bot (AIogram 3)

- Greets user with a menu + WebApp button
- Accepts quick expense text like: `coffee 25000`
- Voice notes: stub (plug STT later)

Run:
```bash
cd bot
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```
